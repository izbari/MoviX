import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  image: {height: 50, width: 50},
  cardContainer: {height: 50, width: 50},
  listHeader: {fontSize: 20, fontWeight: 'bold', color: '#000'},
  listHeaderContainer:{
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    margin: 10,
  }
});
export default style;
