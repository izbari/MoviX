import { StyleSheet } from 'react-native'
const styles = StyleSheet.create({
    movieGenre: {
        color: '#989898',
        fontSize: 12,
        marginTop: 5,
        fontWeight: 'bold',
      },

});
export default styles;